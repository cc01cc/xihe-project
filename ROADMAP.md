# xihe ROADMAP

> **性质**：活文档，修订以 git 历史为准；方向不等于承诺，存疑项如实标注。
>
> **怎么读**：§1 为已落地能力（可对代码验证）；§2-§4 为探索方向（状态见图例）；§5 为待定的开放问题。

**状态图例**：`[OK]` 已实现（对得上代码，可验证） · `[>>]` 进行中 · `[INFO]` 调研中（方向已明确，设计未定型） · `[?]` 开放问题（连方向都可能调整）

---

## 1. 已落地的基础能力 [OK]

来源：`README.md` 与截至 2026-09 已完成的 PLAN（含 PLAN-0317 执行终止语义、PLAN-0323 backlog 清理、PLAN-0326 账本单一写入者）。

- **多 Agent 编排**：角色定义 + 基于角色的访问控制，人类与 Agent 作为对等操作实体统一接入控制面。
- **工具调用**：MCP Server 集成，工具级授权（grant/approval 链路，工具事件带 grantId 三跳时间线）。
- **沙盒执行**：namespace / cgroup / seccomp 隔离的 Runtime（Rust + Axum + bollard）。
- **工作区管理**：按 Agent 隔离的工作区与资源约束；工作区物化带总超时兜底。
- **执行终止语义**：取消链收敛、真实进程组终止、取消时待审批清理（PLAN-0317 收尾）。
- **审计账本**：单一写入者账本，工具事实按 stage 记录、带来源通道标识（channel origin），事件序列在操作行锁下串行（PLAN-0326 收尾）。
- **配置体系**：实例 / 用户 / 工作区三级配置，动态更新，解析优先级 `workspace > user > instance`。

## 2. 守卫体系（Guard） [INFO]

**核心概念**：在 Main Agent 执行过程之外增设一层**守卫 Agent**——它观测执行过程（而非替代执行），从特定维度持续评估，提出修改意见返回 Main Agent，并将验证有效的经验**持久化为 Skill**。

- **时间守卫**：第一性目标是提高时间价值。观测 Main Agent 的执行过程，识别耗时、可优化或可省去的环节，提出修改意见并持久化。
- **算力守卫**：观测上下文与 Token 消耗，研究模型对 Token 的偏好规律，寻找"最省 Token 且不打折开发效率"的操作方式，提出意见并持久化。
- **操作规则守卫**：获取项目工作区内全部规则（Skill / Rules / 文档），实时将 Main Agent 的每个操作与工作区规则匹配、监督；与其他守卫不同，**它有实体且持有拦截权限**——可拦截工作区内 Agent 的违规操作。
- **决策守卫（人格守卫）**：层级最高的守卫，仅次于开发者本人。学习开发者与 Agent 的历史对话，理解开发者的决策性格、习性与偏好，自动替开发者完成部分决策，降低精力消耗。
- **守卫扩展机制**：守卫是开放的 Agent 角色类型，可新增（如"文档守卫"——提升项目文档可读性）。守卫是**独立 Agent 角色**，不隶属于任何 Main Agent 的执行流程；一般持有操作 session 的权限（决策、阻断风险等）。

**与 approval 体系的关系（设计定位）**：守卫的拦截/决策与现有 approval 体系**并行**——即两套并行的 approve 体系：现有体系在 UI 侧、由人操作；守卫是自动化的另一轨，在其授权范围内代行拦截与决策。由此确立两条设计约束：① 两轨的优先级与冲突裁定规则须显式定义；② 守卫的自动决策同样进入审计账本（谁拍板、依据何种规则可查）。决策守卫的自动化程度与授权边界见 §5。

## 3. 多主体通信与树状存储 [INFO]

**第一性原则——多主体（multi-actor）**：xihe 从底层上就是**多开发者 + 多 Agent** 的产品，不是"一个开发者 + 一群 Agent"的单人工具。人是协作主体之一，而非唯一的旁观者：在没有触及底层冲突的日常场景下，人与 Agent 是协作工作的。后续自研版本控制（见 §4 多版本管理）很大程度正是为这条原则服务——多主体并发协作需要一个更成熟、更高可靠的版本控制系统，现有 Git 工作流是为单人/少分支的人类协作设计的。

**决策层级**：用户持有最终决策权，处于最高层级；其下是**决策守卫**（一人之下，万人之上，见 §2）——日常决策由决策守卫代行，人保留最终裁决权。

**存储层**：现存储结构需先调整——引入**树状 / 分层存储**（接近三维树状结构），为执行分叉（branch）与多 Agent 并存做准备。此项是通信机制的前置依赖，尚未实现。

**Agent 拓扑（三种情况）**：

1. Main Agent 分叉出多个 Sub Agent（层级调用）；
2. Main Agent 直接创建一个平级的 Main Agent；
3. 多个平行 Agent **反向合并**为一个新的 Main Agent——某些 PLAN 可并行但通信交互频繁时，合并比维持平行更高效。

**通信机制**：建设类似"公告大厅"的中枢，**人与 Agent 是平等的使用者**——

- Agent 可创建**通道（channel）**或发布**公告**，并周期性把自身状态同步上去；
- **人（开发者）同样可以在公告面板上创建通道、发布自己的开发进度**；
- 订阅是双向的：**人可订阅 Agent 的公告，Agent 也可订阅人发布的公告**（订阅即主动推送，也可浏览全部）；
- 支持一对一、一对多、多对多对话。

## 4. 开发体验与多版本管理 [INFO]

- **多版本管理（跨仓库统一版本管理）**：这一层更合适的名字是"多版本管理"而非"多 Git 管理"。背景问题有两个：
  1. **Git 对多模态二进制资产力不从心**：模型多模态能力（图片 / 视频 / 语音）普及后，工作区内二进制资产越来越多，Git 的文本 diff 范式对它们基本无效（LFS 只是存储卸载，不是语义化版本管理），需要扩展；
  2. **Git 不支持跨仓库管理**：大项目不可能全部塞进同一个仓库，`git worktree` 又是单仓库原语，不存在递归 worktree——主流 harness 的并行隔离全部建立在"一个工作区 = 一个仓库"的隐含假设上。嵌套多仓库工作区（外层仓库 + 多个独立子仓库）中对外层建 worktree 后，**子仓库内修改的是真身**（`.git` 共享），并行隔离直接失效。
  xihe 需要的能力：嵌套仓库边界检测、按子仓库建独立 worktree、维护 agent / 子仓库 / 分支的映射，以及多模态资产的版本化方案。
- **多模态 I/O**：支持视频 / 图片 / 语音等输入输出。内置语音输入会学习工作区的专业词汇与用户输入风格，持续优化识别与理解效果（与守卫体系的"学习开发者"是同一条 personalization 主线）。
- **插件体系**：多版本管理等能力的扩展与外部能力（如 XS 成熟后的并入）都走插件体系承载——插件体系本身是待设计的底座方向。
- **多端支持（含移动端）**：支持多设备接入。这是最初设计 xihe 时的动机之一，也是**前后端分离架构的原因之一**——CP / Agent / Runtime 全部服务端化，UI 只是其中一种客户端，移动端可以承载审批、监控、轻交互等场景，而非完整开发体验。
- **外部通知与平台集成**：接入更多平台与更多类型的通知（IM / 邮件 / Webhook / 移动推送等），把关键事件（审批等待、run 终态、长任务完成、异常等）推送到人所在的工具里，提高平台的可监控能力——不必守在 Web UI 前。
- **git diff 面板与 commit 可读性**：Agent 倾向产出大颗粒 commit，传统逐条审核不合适；需优化 diff 面板呈现与 commit 信息可读性。
- **Markdown 渲染**：工作区内 Markdown 默认渲染而非源码模式（免去每次手动切换），提供更多样式（现有 harness 的 Markdown 渲染样式普遍欠佳）。
- **沙盒 GUI 支持**：Docker 沙盒对 GUI 与浏览器支持很弱，后续增强（含沙盒其他能力项）。
- **快捷操作面板（右侧栏）**：
  - 常用语**插入**输入框（插入而非替换）；
  - 快捷链接：文档 / 目录 / 脚本，快速打开与快速启动；
  - 引用优化：引用内容可**插入正文任意位置**（而非只能整条引用或堆在开头）。
- **多处批注与统一提交**：选中上下文中的内容即可添加批注；同一轮允许多个批注并存；批注完成后**作为一次 chat 统一提交**——先把多处审阅意见标完，再一次性送达，而不是逐条发送打断对话。
- **逐操作超时与可观测**：从"工作区物化总超时"细化到**每个操作独立的超时限制**，且每个操作全程可观测。

## 5. 开放问题 [?]

以下为方向级未定项（次序 / 选型 / 边界），随设计推进修订：

- 存储结构调整与分叉 / 多 Agent 功能的先后次序：先做分叉会倒逼存储重构，先重构则分叉落地更快——未定。
- 决策守卫自动化程度的分寸：分级授权是既定方向（日常代行、人保留最终裁决，见 §3），具体分级与授权边界待细化。
- 多模态资产版本化的方案选型：扩展 Git（LFS 之上加语义层）还是自研内容寻址存储——未定。
- 插件体系的边界：哪些能力进内核、哪些走插件，以及与星枢项目并入的触发条件。
