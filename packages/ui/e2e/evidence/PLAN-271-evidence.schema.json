{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://xihe.dev/schemas/plan-271-evidence.schema.json",
  "title": "PLAN-271 Chat approval and recovery evidence",
  "type": "object",
  "required": ["plan", "generatedAt", "browserConsumer", "browser", "runs", "uncoveredFlows"],
  "properties": {
    "$schema": { "type": "string" },
    "plan": { "const": "PLAN-271" },
    "generatedAt": { "type": "string", "format": "date-time" },
    "browserConsumer": { "const": true },
    "browser": {
      "type": "object",
      "required": ["channel", "workers"],
      "properties": {
        "channel": { "const": "chrome-beta" },
        "workers": { "type": "integer", "minimum": 1 }
      },
      "additionalProperties": false
    },
    "runs": {
      "type": "array",
      "items": { "$ref": "#/$defs/run" }
    },
    "uncoveredFlows": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "status", "reason", "nextEvidence"],
        "properties": {
          "id": { "type": "string" },
          "status": { "enum": ["not-covered", "not-applicable", "blocked"] },
          "reason": { "type": "string" },
          "nextEvidence": { "type": "string" }
        },
        "additionalProperties": false
      }
    }
  },
  "$defs": {
    "run": {
      "type": "object",
      "required": [
        "id", "profile", "command", "status", "testCount", "viewports",
        "pageStates", "actions", "requestResponse", "persistence", "cleanup",
        "artifacts", "approval"
      ],
      "properties": {
        "id": { "type": "string" },
        "profile": { "enum": ["mock", "real-host"] },
        "command": { "type": "string" },
        "status": { "enum": ["passed", "partial", "blocked"] },
        "testCount": { "type": "integer", "minimum": 0 },
        "viewports": { "type": "array", "items": { "type": "string" } },
        "pageStates": { "type": "array", "items": { "type": "string" } },
        "actions": { "type": "array", "items": { "type": "string" } },
        "requestResponse": { "type": "string" },
        "persistence": { "type": "string" },
        "cleanup": { "type": "string" },
        "artifacts": { "type": "array", "items": { "type": "string" } },
        "approval": { "$ref": "#/$defs/approval" }
      },
      "additionalProperties": false
    },
    "approval": {
      "type": "object",
      "required": ["requestId", "runId", "sessionId", "eventOrder", "decision", "state", "downstreamToolCalls"],
      "properties": {
        "requestId": { "type": "string" },
        "runId": { "type": "string" },
        "sessionId": { "type": "string" },
        "eventOrder": { "type": "array", "items": { "type": "string" } },
        "decision": { "enum": ["approve", "reject", "expired", "none", "dispatch_unknown"] },
        "state": { "enum": ["pending", "dispatching", "approved", "rejected", "expired", "dispatch_unknown", "not-applicable"] },
        "responseStatuses": { "type": "array", "items": { "type": "integer", "minimum": 100, "maximum": 599 } },
        "persistedState": { "type": "string" },
        "downstreamToolCalls": { "type": "integer", "minimum": 0 }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
